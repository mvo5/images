[tool.autopep8]
max_line_length = 120

[project]
name = "images"
version = "0.1.0"

dependencies = [
    "vmtest @ git+https://github.com/mvo5/bootc-image-builder.git@paramiko-fixes",
]

[tool.setuptools]
packages = []
